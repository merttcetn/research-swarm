interface:
  display_name: "Research Swarm"
  short_description: "Ephemeral parallel research, minimal main context"
  default_prompt: "Use $research-swarm to research these sources through isolated read-only subagents, return one complete synthesis, and clean up all temporary reports."
